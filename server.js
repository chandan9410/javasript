console.log('hello duniya');
  // console.log(global);
const os = require('os'); // In Node.js, the statement const os = require('os'); serves the purpose of importing the built-in 'os' module.
// This module provides a way to access various operating system-related utility functions and information. Once imported, you can use the os object to access properties and methods provided by the 'os' module.
const math = require('./math')
console.log(math.add(2,4))
// console.log(os.type());
// console.log(os.version());
// console.log(os.homedir());
